#!/usr/bin/env bash
# ═══════════════════════════════════════════════════════════════════════════════
#  cfd-archiver — Wrapper pour archiver les résultats CFD
# ═══════════════════════════════════════════════════════════════════════════════
#
#  Ce script permet de lancer deplacer_resultats.sh depuis n'importe où pour
#  archiver les résultats de calculs CFD.
#
#  Usage: cfd-archiver [OPTIONS] <SOURCE_DIR> <DEST_DIR>
#
#  Auteur : KL
#  Licence : MIT
# ═══════════════════════════════════════════════════════════════════════════════

set -euo pipefail
IFS=$'\n\t'

# ══════════════════════════════════════════════════════════════════════════════
#  🔧 RÉSOLUTION DE CFD_FRAMEWORK
# ══════════════════════════════════════════════════════════════════════════════

# Résoudre CFD_FRAMEWORK si non défini
if [[ -z "${CFD_FRAMEWORK:-}" ]]; then
  # Essayer depuis le chemin du script
  SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
  CFD_FRAMEWORK="$(cd "${SCRIPT_DIR}/.." && pwd)"
  
  export CFD_FRAMEWORK
fi

# Vérifier que CFD_FRAMEWORK existe
if [[ ! -d "$CFD_FRAMEWORK" ]]; then
  echo "ERREUR: Répertoire CFD_FRAMEWORK introuvable: $CFD_FRAMEWORK" >&2
  exit 1
fi

# ══════════════════════════════════════════════════════════════════════════════
#  🚀 LANCEMENT DU SCRIPT D'ARCHIVAGE
# ══════════════════════════════════════════════════════════════════════════════

# Appeler deplacer_resultats.sh avec tous les arguments
exec "${CFD_FRAMEWORK}/scripts/archivage/deplacer_resultats.sh" "$@"
